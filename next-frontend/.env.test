# Versionado de propósito (next-frontend-env-config/TD-05). O host é fictício e não
# resolve em lugar nenhum: qualquer request que escape do MSW falha ruidosamente em
# vez de vazar para o `nestjs-api` real. Carregamento e cascata: ver a skill
# `testing-guide-next-frontend` → `references/external-systems.md`.
API_BASE_URL=http://nestjs-api.test:3000
