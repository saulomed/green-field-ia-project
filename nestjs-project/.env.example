# Application
PORT=3000
NODE_ENV=development

# Database (use Docker Compose service name, never localhost)
DB_HOST=db
DB_PORT=5432
DB_USER=streamtube
DB_PASSWORD=streamtube
DB_NAME=streamtube

# Mail (use Docker Compose service name, never localhost)
MAIL_HOST=mailpit
MAIL_PORT=1025
